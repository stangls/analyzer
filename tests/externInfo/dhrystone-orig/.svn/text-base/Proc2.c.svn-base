#include "dry.h"

void Proc2(OneToFifty *IntParIO)
{
        REG OneToFifty          IntLoc;
        REG Enumeration         EnumLoc;

        IntLoc = *IntParIO + 10;
        for(;;)
        {
                if (Char1Glob == 'A')
                {
                        --IntLoc;
                        *IntParIO = IntLoc - IntGlob;
                        counter+=13;
                        EnumLoc = Ident1;
                }
                        counter+=17;
                if (EnumLoc == Ident1)
                        break;
        }
}
